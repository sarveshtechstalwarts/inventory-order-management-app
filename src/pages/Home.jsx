const Home = () => {
    return (
        <div style={{ padding: '2rem' }}>
            <h1>Welcome to React App</h1>
            <p>This is a clean repository with feature-based architecture.</p>
        </div>
    );
};

export default Home;
